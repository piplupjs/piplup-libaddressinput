{
  "name": "node-cli-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Format, validate and normalize addresses from the command line — shows @piplup/libaddressinput used with zero UI.",
  "scripts": {
    "start": "tsx src/main.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@piplup/libaddressinput": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
