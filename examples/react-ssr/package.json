{
  "name": "react-ssr-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Load region data on the server, PreloadSupplier.export() it into the page, hydrate PreloadSupplier.from() on the client — no second network round trip.",
  "scripts": {
    "dev": "tsx server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@piplup/libaddressinput": "workspace:*",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "tsx": "^4.19.0",
    "vite": "^6.0.0"
  }
}
